id: io/led
name: LED
kind: Io

interfaces:
  - name: in
    kind: digital.sink
    protocol: yantra.bit.v1

  - name: pin
    kind: board.output
    protocol: yantra.board.pin.v1

notes:
  - Logical LED output block.
  - The physical LED pin is selected by the build descriptor.
